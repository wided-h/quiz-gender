<p>"Masculin (m) ou féminin (f) ? Devinez le genre,
  entrez votre choix"</p>
<p id="word"></p>
<button id='guess_f'>f</button>
<button id='guess_m'>m</button>
<p id="comments"></p>
<script>
    let words = JSON.parse('<%=@words.to_json.html_safe %>');
    let word = Object.keys(words)[0];
    document.getElementById("word").innerHTML= word;
    let gender= Object.values(words)[0];
    let score = 0;
    let guessF = document.getElementById('guess_f')
    let guessM = document.getElementById('guess_m')
    let commentSection=document.getElementById('comments')
    let guess = [guessF, guessM];
    guess.forEach((guess) => {

        guess.addEventListener('click',function (event){
            console.log(3)
            let clickedElement=event.target;
            console.log(4)
            if (guess === gender) {
                console.log(5)
                let clickedElementDataset=clickedElement.dataset;
                commentSection.innerText=`ta réponse est juste `;
                console.log(6)
                score += 1;

            } else {
                let clickedElementDataset=clickedElement.dataset;
                console.log(7)
                commentSection.innerText=`ta réponse est fausse`;
            }
console.log(8)
        })
    })
</script>
<br/>
<a href=<%= genders_final_score_path %>>votre score est </a>
<p>&copy; nanou déc. 2020</p>
